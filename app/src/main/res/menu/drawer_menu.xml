<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item  android:id="@+id/nav_observer"
            android:icon="@drawable/ic_flag_activity"
            android:title="Tracker" />
        <item  android:id="@+id/nav_train"
            android:icon="@drawable/ic_train"
            android:title="Train" />
   </group>

    <item android:title="Actions">
        <menu>
            <item  android:id="@+id/nav_reset"
                android:icon="@drawable/ic_reset"
                android:title="Reset" />
            <item  android:id="@+id/nav_save"
                android:icon="@drawable/ic_save"
                android:title="Save to CSV" />
        </menu>
    </item>

</menu>